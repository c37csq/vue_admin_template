import { LAYOUT } from '@/router/constant';

const route = {
  path: '/home',
  name: 'financialSettlementPayment',
  component: LAYOUT,
  meta: {
    title: '财务结算付款',
    loaded: true,
  },
};

export default route;
