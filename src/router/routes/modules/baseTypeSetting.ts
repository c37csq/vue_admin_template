import { LAYOUT } from '@/router/constant';

const route = {
  path: '/home',
  name: 'baseTypeSetting',
  component: LAYOUT,
  meta: {
    title: '基础类型设置',
    loaded: true,
  },
};

export default route;
