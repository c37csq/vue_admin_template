import { LAYOUT } from '@/router/constant';

const route = {
  path: '/home',
  name: 'financialSettlementCollection',
  component: LAYOUT,
  meta: {
    title: '财务结算收款',
    loaded: true,
  },
};

export default route;
