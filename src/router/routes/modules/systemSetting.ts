import { LAYOUT } from '@/router/constant';

const route = {
  path: '/home',
  name: 'systemSetting',
  component: LAYOUT,
  meta: {
    title: '系统设置',
    loaded: true,
  },
};

export default route;
