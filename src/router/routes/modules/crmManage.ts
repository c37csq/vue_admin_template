import { LAYOUT } from '@/router/constant';

const route = {
  path: '/home',
  name: 'crmManage',
  component: LAYOUT,
  meta: {
    title: 'CRM客户资源管理',
    loaded: true,
  },
};

export default route;
