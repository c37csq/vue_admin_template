import { LAYOUT } from '@/router/constant';

const route = {
  path: '/home',
  name: 'expenseSetting',
  component: LAYOUT,
  meta: {
    title: '费用设置',
    loaded: true,
  },
};

export default route;
