import { LAYOUT } from '@/router/constant';

const route = {
  path: '/home',
  name: 'businessManage',
  component: LAYOUT,
  meta: {
    title: '客户资源管理',
    loaded: true,
  },
};

export default route;
